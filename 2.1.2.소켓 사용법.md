# 2.1.2 소켓 사용법

## 📌 소켓의 개설

소켓을 이용하여 IP packet을 송수신할 때 다음 정보가 필요하다.

- **Protocol** (TCP or UDP)
- **Local IP address**
- **Local port number**
- **Remote IP address**
- **Remote port number**

소켓의 개설은 **`socket()` 시스템 콜**을 사용한다.

---

## 🛠 `socket()` 시스템 콜

```c
#include <sys/socket.h>

int socket(int domain, int type, int protocol);
// Returns: nonnegative descriptor if OK, -1 on error
```

### 매뉴얼 페이지 확인

`man socket` 명령으로 함수의 시그니처와 필요한 헤더 파일을 확인할 수 있다.

<img width="589" height="193" alt="socket_man_page" src="https://github.com/user-attachments/assets/27172b5d-ed78-4487-9722-0bbcde55fee5" />


### 인자 설명

#### `domain` — 통신 도메인

| 값 | 설명 |
|----|------|
| `PF_INET` | IPv4 protocols |
| `PF_INET6` | IPv6 protocols |
| `PF_UNIX` | Unix domain protocols |
| `PF_NS` | XEROX network protocols |

#### `type` — 소켓의 종류

| 값 | 설명 |
|----|------|
| `SOCK_STREAM` | stream socket (TCP) |
| `SOCK_DGRAM` | datagram socket (UDP) |
| `SOCK_RAW` | raw socket |

#### `protocol`

> 일반적으로 **`0`** 으로 지정한다. (단, raw socket은 예외)

---

## 📁 헤더 파일 위치

C 프로그램 작성 시 헤더 파일을 `include` 하는 것이 필요하며, 시스템에서 헤더 파일은 다음 디렉토리에 위치한다.

| 디렉토리 | 포함된 헤더 파일 |
|----------|------------------|
| `/usr/include` | `stdio.h`, `stdlib.h` 등 C 언어 기본 헤더 |
| `/usr/include/sys` | `socket.h`, `types.h` 등 시스템 관련 헤더 |
| `/usr/include/netinet` | `ip.h`, `tcp.h` 등 인터넷 관련 헤더 |

> 💡 **Tip**: `man` 페이지를 이용하여 컴파일 시 필요한 헤더 파일을 알 수 있다.
> ```bash
> $ man socket
> ```

---

## 🏷 소켓 주소 구조체

소켓을 이용하는 server 또는 client의 구체적인 주소를 표현하는 구조체이다.

### 소켓 주소의 구성

- **address family**
- **IP address**
- **port number**

### 1) Generic socket address structure

```c
struct sockaddr {
    u_short  sa_family;       /* address family: AF_xxx value */
    char     sa_data[14];     /* protocol-specific address */
};
```

### 2) IPv4 주소 구조체

```c
struct in_addr {
    u_long  s_addr;           /* 32-bit IPv4 address */
                              /* network byte ordered */
};
```

### 3) IPv4용 소켓 주소 구조체

```c
struct sockaddr_in {
    short            sin_family;   /* address family: AF_xxx value */
    u_short          sin_port;     /* 16-bit TCP or UDP port number */
                                   /* network byte ordered */
    struct in_addr   sin_addr;     /* 32-bit IPv4 address */
                                   /* network byte ordered */
    char             sin_zero[8];  /* unused */
};
```

### Address Family 값

| 값 | 설명 |
|----|------|
| `AF_INET` | IPv4 protocols |
| `AF_UNIX` | Unix domain protocols |
| `AF_NS` | XEROX network protocols |

> 📝 **참고**: `PF_xxx`(Protocol Family)와 `AF_xxx`(Address Family)는 거의 동일한 값을 갖는다. 일반적으로 `socket()` 호출에는 `PF_xxx`를, 주소 구조체에는 `AF_xxx`를 사용한다.

---

## 🔄 서버 / 클라이언트 통신 흐름

TCP 기반 서버-클라이언트 프로그램의 전형적인 시스템 콜 호출 순서이다.

<img width="492" height="577" alt="server_client_flow" src="https://github.com/user-attachments/assets/a93b6511-6b13-4e74-80c6-62b3603f1870" />


### 서버 측 흐름

| 순서 | 시스템 콜 | 역할 |
|:---:|-----------|------|
| 1 | `socket()` | 소켓 생성 |
| 2 | `bind()` | 소켓에 로컬 IP/포트 할당 |
| 3 | `listen()` | 연결 요청 대기 상태로 전환 |
| 4 | `accept()` | 클라이언트의 연결 요청 수락 |
| 5 | `send()` / `recv()` | 데이터 송수신 |
| 6 | (`close()`) | 연결 종료 |

### 클라이언트 측 흐름

| 순서 | 시스템 콜 | 역할 |
|:---:|-----------|------|
| 1 | `socket()` | 소켓 생성 |
| 2 | `connect()` | 서버에 연결 요청 |
| 3 | `send()` / `recv()` | 데이터 송수신 |
| 4 | `close()` | 연결 종료 |

### 통신 단계 요약

1. **연결 요청**: 클라이언트의 `connect()` → 서버의 `accept()`
2. **데이터 송수신**: 양쪽이 `send()` / `recv()`로 데이터를 주고받음
3. **종료**: 클라이언트의 `close()` → 서버에서 종료 감지

---

## ✅ 핵심 요약

1. `socket()` 시스템 콜은 **`domain`, `type`, `protocol`** 세 인자를 받아 소켓 디스크립터를 리턴한다.
2. `protocol` 인자는 보통 **`0`** 으로 둔다 (raw socket 제외).
3. 헤더 파일은 `/usr/include`, `/usr/include/sys`, `/usr/include/netinet` 디렉토리에 분산되어 있다.
4. 소켓 주소는 일반 구조체 **`sockaddr`**, IPv4 전용 **`sockaddr_in`** 으로 표현된다.
5. 서버는 `socket → bind → listen → accept` 순으로, 클라이언트는 `socket → connect` 순으로 통신을 시작한다.
