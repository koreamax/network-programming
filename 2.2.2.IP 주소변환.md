# 2.2.2 IP 주소 변환

## 📌 개요

소켓 프로그램에서는 다음 세 가지 IP 주소 표현 방식을 **변환 함수들을 이용해 서로 변환**시킬 수 있다.

- **Domain name** (예: `cc.kangwon.ac.kr`)
- **32-bit IPv4 address (binary)** (예: `11000000 11001011 10010000 00001011`)
- **Dotted decimal** (예: `192.203.144.11`)

### IP 주소 표현 방식 및 상호변환 함수

<img width="692" height="255" alt="ip_address_conversion" src="https://github.com/user-attachments/assets/d9cf9866-0c7d-4c4f-94b9-6a8fd27c78d0" />


| 변환 방향 | 함수 |
|-----------|------|
| Domain name → IP address | `gethostbyname()` |
| IP address → Domain name | `gethostbyaddr()` |
| Dotted decimal → Binary | `inet_pton()` |
| Binary → Dotted decimal | `inet_ntop()` |

---

## 🛠 `inet_pton()` / `inet_ntop()`

문자열(presentation) 형식과 바이너리(numeric) 형식 간의 변환 함수.

```c
#include <sys/types.h>
#include <sys/socket.h>
#include <arpa/inet.h>

int          inet_pton(int af, const char *src, void *dst);
const char  *inet_ntop(int af, const void *src, char *dst, size_t cnt);
```

| 함수 | 변환 방향 | 설명 |
|------|-----------|------|
| `inet_pton()` | **p**resentation → **n**umeric | dotted decimal 문자열 → binary |
| `inet_ntop()` | **n**umeric → **p**resentation | binary → dotted decimal 문자열 |

> 💡 함수명의 `p`는 presentation(문자열), `n`은 numeric(바이너리)를 의미한다.

---

## 🔄 Address Conversion Functions (구버전)

```c
#include <arpa/inet.h>

in_addr_t  inet_addr(const char *strptr);
// Returns: 32-bit binary network byte ordered IPv4 address;
//          INADDR_NONE if error

char      *inet_ntoa(struct in_addr inaddr);
// Returns: pointer to dotted-decimal string
```

| 함수 | 변환 방향 | 설명 |
|------|-----------|------|
| `inet_addr()` | dotted decimal → binary | 32-bit network byte order로 리턴 |
| `inet_ntoa()` | binary → dotted decimal | 문자열 포인터 리턴 |

> ⚠️ `inet_addr()`, `inet_ntoa()`는 IPv4만 지원하므로, IPv6 호환을 위해서는 **`inet_pton()`, `inet_ntop()` 사용을 권장**한다.

---

## 🌐 Domain Address Conversion Functions

도메인 네임과 IP 주소 사이의 변환 함수.

```c
#include <netdb.h>

struct hostent *gethostbyname(const char *hostname);
struct hostent *gethostbyaddr(const char *addr, size_t len, int family);
// Both return: nonnull pointer if OK, NULL on error with h_errno set
```

| 함수 | 변환 방향 |
|------|-----------|
| `gethostbyname()` | hostname → IP address |
| `gethostbyaddr()` | IP address → hostname |

> ⚠️ **참고**: `gethostbyaddr()`는 **reverse DNS**가 수행되어야 하지만, 대부분의 DNS는 보안 문제로 인하여 reverse DNS 기능을 제공하지 않는다.

---

## 🏷 `hostent` 구조체

인터넷 호스트를 나타내는 구조체.

```c
struct hostent {
    char    *h_name;       /* official (canonical) name of host */
    char   **h_aliases;    /* pointer to array of pointers to alias names */
    int      h_addrtype;   /* host address type : AF_INET or AF_INET6 */
    int      h_length;     /* length of address : 4 or 16 */
    char   **h_addr_list;  /* ptr to array of ptrs with IPv4 or IPv6 addrs */
};

#define h_addr  h_addr_list[0]   /* first address in list */
```

### 필드 설명

| 필드 | 설명 |
|------|------|
| `h_name` | 호스트의 공식(canonical) 이름 |
| `h_aliases` | 별칭 이름들의 배열 포인터 |
| `h_addrtype` | 주소 타입: `AF_INET` 또는 `AF_INET6` |
| `h_length` | 주소 길이: 4(IPv4) 또는 16(IPv6) |
| `h_addr_list` | IPv4 / IPv6 주소들의 배열 포인터 |

### `hostent` 구조체 메모리 구성

<img width="809" height="375" alt="hostent_structure" src="https://github.com/user-attachments/assets/1b0ca765-013e-424c-a185-67444f0a14dd" />

위 그림에서 볼 수 있듯이, `hostent` 구조체는 다음과 같이 구성된다.

- **`h_name`** → 공식 호스트 이름 문자열을 가리킴 (예: `official hostname \0`)
- **`h_aliases`** → 별칭 문자열 포인터 배열 → 각 `alias#1`, `alias#2`, …, `NULL`
- **`h_addrtype`** = `AF_INET`
- **`h_length`** = `4` (IPv4의 경우 4바이트)
- **`h_addr_list`** → `in_addr` 구조체 포인터 배열 → 각 `IP addr#1`, `IP addr#2`, `IP addr#3`, …, `NULL`

> 💡 한 호스트가 **여러 개의 별칭**과 **여러 개의 IP 주소**를 가질 수 있으므로 둘 다 배열 형태로 관리된다.

---

## ✅ 핵심 요약

1. IP 주소는 **domain name / binary / dotted decimal** 세 가지로 표현되며 상호 변환 함수가 존재한다.
2. **`inet_pton()` / `inet_ntop()`** 은 문자열과 바이너리 간 변환 함수이며 IPv4/IPv6를 모두 지원한다.
3. **`inet_addr()` / `inet_ntoa()`** 는 IPv4 전용 구버전 함수이다.
4. **`gethostbyname()`** 은 도메인 → IP, **`gethostbyaddr()`** 는 IP → 도메인 변환에 쓰이며, 후자는 reverse DNS가 필요하다.
5. **`hostent`** 구조체는 호스트의 공식 이름, 별칭들, 주소 타입과 길이, 그리고 여러 개의 IP 주소를 모두 담는다.
