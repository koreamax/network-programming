# 2.3.1 TCP 클라이언트 절차

## 📌 개요

소켓을 이용하는 일반적인 **클라이언트 프로그램 작성 절차**를 다룬다.

> 🎯 유닉스 혹은 리눅스 서버에서 제공하는 **daytime** 및 **echo** 서비스를 이용하는 클라이언트 프로그램을 예제로 한다.

---

## 🔄 TCP Client / Server 통신 흐름

<img width="860" height="600" alt="tcp_client_server_flow" src="https://github.com/user-attachments/assets/115ac0af-0ecb-4c24-bef2-c02796e6b17b" />


### 클라이언트 측 절차

| 순서 | 시스템 콜 | 역할 |
|:---:|-----------|------|
| 1 | `socket()` | 소켓 생성 |
| 2 | `sockaddr_in` 작성 | 서버의 주소 구조체 준비 |
| 3 | `connect()` | 서버에 연결 요청 (TCP 3-way handshake) |
| 4 | `send()` | 데이터(요청) 전송 |
| 5 | `recv()` | 서버로부터 데이터(응답) 수신 |
| 6 | `send()` | end-of-file 알림 |
| 7 | `close()` | 소켓 종료 |

### 서버 측 절차 (참고)

| 순서 | 시스템 콜 | 역할 |
|:---:|-----------|------|
| 1 | `socket()` | 소켓 생성 |
| 2 | `bind()` | well-known port에 바인딩 |
| 3 | `listen()` | 연결 대기 상태 |
| 4 | `accept()` | 클라이언트 연결 수락 (block) |
| 5 | `recv()` → process → `send()` | 요청 처리 후 응답 |
| 6 | `recv()` | EOF 수신 |
| 7 | `close()` | 소켓 종료 |

---

## 🔌 `socket()` 호출과 소켓 인터페이스

`socket()` 호출 시 응용 프로그램은 **소켓 디스크립터**를 리턴받고, 소켓 인터페이스에는 프로토콜과 주소 정보를 담을 빈 자리가 마련된다.

<img width="732" height="566" alt="socket_call_relation" src="https://github.com/user-attachments/assets/ff73af8f-9f36-4640-9fe5-3b332a95a462" />


- **Application program** → `socket()` 호출
- **Socket descriptor** 리턴
- **Socket interface**에는 다음 슬롯이 준비됨:
  - `protocol`
  - `local IP address` / `local port number`
  - `remote IP address` / `remote port number`

> 📝 `socket()` 호출 직후에는 **프로토콜만 결정**되어 있고, 로컬/원격 주소는 아직 비어있다.

---

## 🔗 `connect()` 함수

```c
int connect(
    int s,                         // 서버와 연결시킬 소켓 번호
    const struct sockaddr *addr,   // 상대방 서버의 소켓 주소 구조체
    int addrlen                    // 구조체 *addr의 크기
);
// return 0 if success, return -1 if fail
```

### `connect()` 호출과 소켓 주소의 관계

`connect()` 호출 시 **상대방 서버의 IP/Port 정보가 소켓 인터페이스에 채워지며**, 동시에 로컬 IP/Port도 자동으로 결정된다.

<img width="692" height="582" alt="connect_call_relation" src="https://github.com/user-attachments/assets/bdcec3a1-a3cc-43a9-b13c-ba80127f7acf" />


- 클라이언트가 `connect()`를 호출하면 인자로 넘긴 `sockaddr` 구조체로부터 **remote IP / remote port**가 소켓 인터페이스에 기록됨
- **local IP / local port**는 시스템이 자동으로 할당
- 이후 TCP 3-way handshake를 거쳐 연결이 확립됨

---

## 📤 `send()` / `recv()` 함수

데이터 송수신을 위한 함수.

```c
#include <sys/socket.h>

ssize_t recv(int sockfd, void *buff, size_t nbytes, int flags);
ssize_t send(int sockfd, const void *buff, size_t nbytes, int flags);
// Both return: number of bytes read or written if OK, -1 on error
```

### 인자 설명

| 인자 | 설명 |
|------|------|
| `sockfd` | socket descriptor |
| `buff` | 버퍼 포인터 (read 또는 write 대상) |
| `nbytes` | 송신 또는 수신할 바이트 수 |
| `flags` | I/O 함수에 대한 플래그 (이 장에서는 **`0`** 으로 설정) |

### `read()` / `write()` 와의 관계

```c
int write(int sockfd, const void *buff, int nbytes);
int read(int sockfd, void *buff, int nbytes);
```

> 💡 소켓은 파일 디스크립터처럼 다룰 수 있으므로 `read()` / `write()` 함수도 사용 가능하다. 다만 소켓 전용 옵션(`flags`)을 사용하려면 `send()` / `recv()`를 써야 한다.

---

## 🔚 `close()` — 소켓 종료

소켓의 사용을 마치려면 해당 **소켓번호를 지정하여 `close()`를 호출**한다.

### 동작 규칙

- `close()`는 **클라이언트나 서버 중 누구나 먼저 호출**할 수 있다.
- `close()`를 호출한 시점의 **송신버퍼에 남아 있는 데이터** 혹은 **네트워크 내에서 전달중인 데이터**는 기본적으로 모두 전송이 완료된 후에 TCP 연결이 종료된다.

> 🛡 즉, `close()`는 즉시 끊는 것이 아니라 **graceful shutdown** 방식으로 안전하게 연결을 정리한다.

---

## ✅ 핵심 요약

1. TCP 클라이언트는 **`socket() → connect() → send()/recv() → close()`** 순으로 동작한다.
2. `socket()` 호출 직후에는 프로토콜만 결정되고, **`connect()` 호출 시점에 로컬/원격 주소가 모두 채워진다**.
3. `connect()`는 서버의 `sockaddr` 구조체와 그 크기를 인자로 받아 TCP 3-way handshake를 수행한다.
4. `send()` / `recv()` 의 `flags` 인자는 일반적으로 **`0`** 으로 둔다. (소켓을 파일처럼 `read()` / `write()`로 다룰 수도 있음)
5. `close()`는 **클라이언트/서버 누구나 먼저 호출 가능**하며, 송신 중인 데이터가 모두 전달된 후 연결이 종료된다.
