# 2.4 TCP 서버 프로그램

## 📌 개요

소켓을 이용하는 **connection-oriented server 프로그램** 작성 절차를 다룬다.

> 🎯 **echo service**를 제공하는 connection-oriented server 프로그램을 예제로 한다.

---

## 🔄 Iterative TCP Server 흐름

<img width="905" height="621" alt="iterative_tcp_server_flow" src="https://github.com/user-attachments/assets/5cbc5be0-4708-43dc-bdfb-53b87d79a041" />


서버는 `socket → bind → listen → accept`의 순서로 클라이언트의 요청을 기다리고, 연결이 수립되면 `recv → process → send`를 반복하며 서비스를 제공한다.

---

## 🛠 서버의 소켓 관련 함수

| 함수 | 역할 |
|------|------|
| **`socket()`** | 응용 프로그램 내에서 유일한 socket descriptor 배정 |
| **`bind()`** | socket descriptor와 socket address (IP address and port number) 연결 |
| **`listen()`** | Clients의 연결 요청을 기다림 |
| **`accept()`** | Clients의 연결 요청 수락. 통신을 위한 **새로운 socket descriptor** 반환 |

> 💡 Clients에게 서비스를 제공하기 위해서는 **clients가 알고 있는 서버의 socket address와 socket descriptor를 `bind()` 시켜야 한다**.

---

## 1️⃣ `bind()` 함수

```c
int bind(int s, struct sockaddr *addr, int addrlen);
```

### `bind()` 호출 시 소켓번호와 소켓 주소의 관계

<img width="766" height="549" alt="bind_call_relation" src="https://github.com/user-attachments/assets/8f5f9733-7b20-4255-930c-5120dd90c8fa" />


- `socket()`으로 받은 **socket descriptor**가 있는 상태에서
- `bind()` 호출 시 **local IP address / local port number**가 소켓 인터페이스에 채워짐
- 이 시점에서 **remote 측 정보는 아직 비어있음** (클라이언트가 연결해와야 채워짐)

| 인자 | 설명 |
|------|------|
| `s` | socket descriptor |
| `addr` | 바인딩할 소켓 주소 구조체 포인터 |
| `addrlen` | 구조체의 크기 |

---

## 2️⃣ `listen()` 함수

```c
#include <sys/socket.h>

int listen(int s, int log);
// Returns: 0 if OK, -1 on error
```

| 인자 | 설명 |
|------|------|
| `s` | socket descriptor |
| `log` | maximum number of clients waiting for connection (대기 큐 크기) |

### Listening Socket의 두 큐

<img width="834" height="401" alt="listen_two_queues" src="https://github.com/user-attachments/assets/f2c2de4b-e6d0-4683-b40f-4840c3d37164" />


TCP는 listening socket을 위해 **두 개의 큐**를 유지한다.

| 큐 | TCP 상태 | 설명 |
|----|----------|------|
| **Incomplete connection queue** | `SYN_RCVD` | 클라이언트의 SYN을 받았지만 3-way handshake가 아직 완료되지 않은 연결 |
| **Completed connection queue** | `ESTABLISHED` | 3-way handshake가 완료되어 `accept()`로 가져갈 수 있는 연결 |

> ⚠️ **두 큐의 합이 `log` 값을 초과할 수 없다** (sum of both queues cannot exceed log).

### 큐 동작 흐름

1. 클라이언트의 **SYN 도착** → incomplete queue에 엔트리 생성
2. **3-way handshake 완료(3WHS complete)** → completed queue로 이동
3. 서버가 **`accept()` 호출** → completed queue에서 엔트리를 꺼냄

---

## 3️⃣ TCP 3-way Handshake 절차

<img width="886" height="342" alt="three_way_handshake" src="https://github.com/user-attachments/assets/4dd1f86d-3766-4730-9f8c-5ab03eb2133a" />


### 단계별 동작

| 단계 | 방향 | 패킷 | 클라이언트 | 서버 |
|:---:|:---:|------|------------|------|
| 1 | Client → Server | `SYN J` | `connect()` 호출 | incomplete queue에 엔트리 생성 |
| 2 | Server → Client | `SYN K, ack J+1` | (RTT 대기) | — |
| 3 | Client → Server | `ack K+1` | `connect()` 리턴 | 엔트리를 completed queue로 이동 → `accept()` 리턴 가능 |

### 핵심 포인트

- **RTT (Round Trip Time)** 만큼의 지연이 양쪽에서 발생
- 서버 입장에서 `accept()`는 3-way handshake가 완료된 후에야 리턴됨
- 클라이언트의 `connect()`는 두 번째 패킷(SYN+ACK) 수신 시 리턴됨

---

## 4️⃣ `accept()` 함수

```c
int accept(int s, struct sockaddr *addr, int *addrlen);
```

### `accept()` 호출 시 얻는 소켓 주소 정보와 새로운 소켓

<img width="626" height="546" alt="accept_call_relation" src="https://github.com/user-attachments/assets/2ef8bb92-159d-4e4a-97b5-26770360b0f9" />

`accept()` 호출 시:

- **기존 socket descriptor** (listening socket)는 그대로 유지됨
- **새로운 socket descriptor**가 생성되어 리턴됨 → 이것이 클라이언트와의 실제 통신용
- 새 소켓에는 **remote IP address / remote port number**가 채워짐
- `addr` 인자를 통해 **클라이언트의 주소 정보**를 받을 수 있음

| 인자 | 설명 |
|------|------|
| `s` | listening socket descriptor |
| `addr` | 클라이언트의 주소 정보를 받을 구조체 포인터 |
| `addrlen` | 구조체 크기를 담은 변수의 포인터 (값-결과 인자) |

> 💡 **두 개의 소켓**: `accept()` 후 서버는 **listening socket** (계속 다른 클라이언트의 연결 대기용)과 **connected socket** (현재 클라이언트와의 통신용) 두 개를 갖게 된다.

---

## ✅ 핵심 요약

1. TCP 서버는 **`socket() → bind() → listen() → accept()`** 순으로 클라이언트의 연결을 받을 준비를 한다.
2. **`bind()`** 는 socket descriptor에 local IP/Port를 연결시키며, 클라이언트가 알고 있는 주소로 바인딩해야 한다.
3. **`listen()`** 은 두 개의 큐(**incomplete** / **completed**)를 유지하며, 두 큐의 합이 `log` 값을 초과할 수 없다.
4. **TCP 3-way handshake**가 완료되어야 `accept()`가 리턴되며, 이때 비로소 통신이 가능해진다.
5. **`accept()`는 새로운 socket descriptor를 리턴**하며, 서버는 listening socket과 connected socket 두 개를 갖게 된다.
