# 2.6 Connected UDP

## 📌 개요

UDP 소켓을 통해서 통신할 상대방이 **한 곳으로 고정되어 있을 경우**, 처리 속도 향상을 위하여 **connected UDP**를 사용할 수 있다.

---

## 🔗 동작 방식

### 핵심 메커니즘

- 일반 UDP 소켓에 **`connect()` 함수를 호출**하여 커널 내부에 **상대방 소켓 주소와 UDP 소켓을 연결**하도록 한다.
- ⚠️ **TCP와 같은 3-way handshake가 이루어지는 것은 아니다.**
- 단순히 커널이 상대방 주소를 기억해두고, 이후 송수신 시 매번 주소를 지정할 필요가 없게 만드는 것이다.

### 데이터 송수신 함수

| 동작 | Connected UDP | 일반 UDP |
|------|---------------|----------|
| **데이터 전송** | `send()`, `write()` | `sendto()` |
| **데이터 수신** | `read()`, `recv()` | `recvfrom()` |

> 💡 connected UDP에서는 상대방 주소가 이미 커널에 등록되어 있으므로, **TCP에서 쓰는 것과 동일한 송수신 함수**(`send`/`recv`/`read`/`write`)를 사용할 수 있다.

---

## ⚖️ Connected UDP vs 일반 UDP

| 항목 | 일반 UDP | Connected UDP |
|------|----------|---------------|
| 사전 호출 | 없음 | `connect()` 호출 필요 |
| 3-way handshake | — | **없음** (TCP와 다름) |
| 데이터 전송 | `sendto()` (매번 주소 지정) | `send()`, `write()` (주소 불필요) |
| 데이터 수신 | `recvfrom()` (매번 주소 받음) | `read()`, `recv()` |
| 통신 상대 | 매 호출마다 변경 가능 | **한 곳으로 고정** |
| 처리 속도 | 일반 | **향상됨** |

---

## 🎯 사용 시점

### Connected UDP가 유리한 경우

- UDP 통신 상대방이 **하나로 고정**되어 있고, 여러 번 데이터를 주고받아야 할 때
- 매번 `sendto()` / `recvfrom()`으로 주소를 지정하는 오버헤드를 줄이고 싶을 때

### 일반 UDP를 써야 하는 경우

- 여러 클라이언트와 통신해야 하는 UDP 서버
- 매번 다른 상대방에게 datagram을 보내야 할 때

---

## ✅ 핵심 요약

1. **Connected UDP**는 통신 상대가 고정되어 있을 때 처리 속도를 높이기 위한 방식이다.
2. UDP 소켓에 **`connect()`를 호출**하여 커널이 상대방 주소를 기억하게 만든다.
3. **TCP와 달리 3-way handshake는 일어나지 않는다** — 단순히 주소를 등록할 뿐이다.
4. 송신은 **`send()` / `write()`**, 수신은 **`read()` / `recv()`** 를 사용한다 (주소 인자 불필요).
5. 매 호출마다 주소를 지정하지 않으므로 **반복 통신 시 오버헤드가 감소**한다.
