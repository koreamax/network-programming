# 2.2.1 바이트 순서 (Byte Order)

## 📌 개요

컴퓨터들마다 **내부에서 숫자를 표현하는 방식이 다를 수 있다**. 따라서 숫자를 네트워크로 전송하기 전과 후에는 **바이트 순서를 맞추는 절차**가 필요하다.

- **Host byte order** — 각 컴퓨터(호스트) 내부의 바이트 순서
- **Network byte order** — 네트워크 전송용 표준 바이트 순서

### 인터넷 주소 표현 방식

| 표현 방식 | 예시 |
|-----------|------|
| Domain name | `www.example.com` |
| 32-bit IPv4 address | `0xD2B5D5C2` |
| Dotted decimal | `210.181.213.194` |

---

## 🔢 Host Byte Order vs Network Byte Order

### Host byte order
컴퓨터의 **CPU 종류에 따라 다르다**.

- **Little-endian byte order** — 80x86 계열
- **Big-endian byte order** — MC68000 계열

### Network byte order
**CPU 종류에 관계없이 big-endian order**를 따른다.

### 시각적 비교: `0xC3E2`의 host byte order



| 주소 | `n` | `n+1` |
|:---:|:---:|:---:|
| **(a) little-endian** (80x86 계열) | `E2` | `C3` |
| **(b) big-endian** (MC68000 계열) | `C3` | `E2` |

> 💡 같은 값 `0xC3E2`라도 메모리에 저장되는 순서가 CPU에 따라 다르다. 네트워크 전송 시에는 반드시 big-endian(network byte order)으로 변환해야 한다.

---

## 🛠 Byte Ordering Functions

```c
#include <netinet/in.h>

uint16_t  htons(uint16_t  host16bitvalue);
uint32_t  htonl(uint32_t  host32bitvalue);
// Both return: value in network byte order

uint16_t  ntohs(uint16_t  net16bitvalue);
uint32_t  ntohl(uint32_t  net32bitvalue);
// Both return: value in host byte order
```

### 함수 이름 규칙

| 약어 | 의미 |
|:---:|------|
| `h` | host |
| `n` | network |
| `s` | short (2 byte) |
| `l` | long (4 byte) |

### 함수별 용도

| 함수 | 크기 | 변환 방향 |
|------|:---:|-----------|
| `htons()` | 2 byte | host → network |
| `ntohs()` | 2 byte | network → host |
| `htonl()` | 4 byte | host → network |
| `ntohl()` | 4 byte | network → host |

#### Unsigned short integer 변환 (2 바이트 크기)
- **`htons()`**: host-to-network 바이트 변환
- **`ntohs()`**: network-to-host 바이트 변환

#### Unsigned long integer 변환 (4 바이트 크기)
- **`htonl()`**: host-to-network 바이트 변환
- **`ntohl()`**: network-to-host 바이트 변환

> 🎯 **사용 예시**: 포트 번호는 `htons()`, IP 주소는 `htonl()`을 사용하여 변환한다.

---

## 🔍 `getservbyname()`

시스템이 현재 지원하는 특정 응용 프로그램의 정보를 알아내는 함수.

- **인자**: 서비스 이름과 프로토콜
- **리턴값**: 서비스와 관련된 각종 정보를 포함하는 **`servent`** 라는 구조체의 포인터

```c
struct servent *getservbyname(const char *name, const char *proto);
```

| 인자 | 설명 |
|------|------|
| `name` | 서비스 이름 (예: `"http"`, `"ftp"`) |
| `proto` | 프로토콜 이름 (예: `"tcp"`, `"udp"`) |

---

## ✅ 핵심 요약

1. **Host byte order**는 CPU에 따라 little-endian 또는 big-endian이다.
2. **Network byte order**는 항상 **big-endian**으로 통일되어 있다.
3. 네트워크로 데이터를 보내기 전과 받은 후에는 **반드시 바이트 순서 변환**이 필요하다.
4. `htons`/`htonl`은 host → network, `ntohs`/`ntohl`은 network → host 방향이다 (`s`=2바이트, `l`=4바이트).
5. `getservbyname()`은 서비스 이름으로부터 포트 번호 등의 정보를 담은 `servent` 구조체를 얻는 함수이다.
